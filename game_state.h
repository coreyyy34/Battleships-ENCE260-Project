#ifndef GAME_STATE_H
#define GAME_STATE_H

typedef enum {
    GAME_STATE_TITLE_SCREEN,
    GAME_STATE_SELECT_PLAYER,
    GAME_STATE_CHOOSE_BOARD,
    GAME_STATE_PLACE_SHIPS,
} GameState_t;

#endif